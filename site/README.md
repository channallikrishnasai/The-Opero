# OPERO landing page

`index.html` is a standalone static landing page. Deploy the `site` directory to Netlify by importing this repository and setting the publish directory to `site`, or drag the folder into Netlify Drop.

No build step or third-party assets are required.

The repository includes `netlify.toml`; when connected to Netlify, it publishes this directory with defensive browser headers.

